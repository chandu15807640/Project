@page
@model LindnerAirlines.Pages.Payments.DeleteModel

@{
    ViewData["Title"] = "Delete";
}
<div class="bgim">
    <h1>Delete a Payment</h1>

    <h3>Are you sure you want to delete this Payment?</h3>
    <div>

        <hr />
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Payment.Amount)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Payment.Amount)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Payment.PaymentMethod)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Payment.PaymentMethod)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Payment.TransactionId)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Payment.TransactionId)
            </dd>
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.Payment.User)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.Payment.User.Email)
            </dd>
        </dl>

        <form method="post">
            <input type="hidden" asp-for="Payment.Id" />
            <input type="submit" value="Delete" class="btn btn-danger" /> |
            <a asp-page="./Index">Back to List</a>
        </form>

        <br />
        <br />
    </div>

</div>
